[master]
{{ master_name }} ansible_host={{ master_ip }} ansible_user=ubuntu ansible_ssh_private_key_file={{ ssh_key_path }}

[workers]
{% for ip in worker_ips %}
spark-worker-{{ loop.index }} ansible_host={{ ip }} ansible_user=ubuntu ansible_ssh_private_key_file={{ ssh_key_path }}
{% endfor %}
